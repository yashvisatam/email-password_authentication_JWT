# email-password_authentication_JWT
This is a Django project with Django REST Framework(DRF). It uses the JSON Web Tokens for authentication. It is done by customising existing UserModel and Serializer. 
